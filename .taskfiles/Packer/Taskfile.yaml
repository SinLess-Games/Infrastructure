version: '3'

vars:
  PACKER_DIR: "Packer"
  DEBIAN_DIR: "{{.PACKER_DIR}}/debian"
  PACKER_LOG: "{{.PACKER_DIR}}/packer-build.log"

tasks:

  default:
    desc: "Show available Packer tasks"
    cmds:
      - task --list
