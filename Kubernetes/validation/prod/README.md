# Production Validation

Use this folder for production verification and troubleshooting assets.

## How To Use It

- `scripts/`
  Run these for phase-specific checks and smoke tests.
- `matrices/`
  Use these as the expected-state reference during rollout and troubleshooting.
- `examples/`
  Keep validation-only example manifests here when they support operational testing.
