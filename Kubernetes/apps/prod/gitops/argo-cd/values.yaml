configs:
  params:
    server.insecure: false
  secret:
    argocdServerAdminPassword: ${ARGOCD_ADMIN_PASSWORD_BCRYPT}
server:
  ingress:
    enabled: false
controller:
  replicas: 1
repoServer:
  replicas: 2
applicationSet:
  replicas: 2
redis:
  enabled: true
