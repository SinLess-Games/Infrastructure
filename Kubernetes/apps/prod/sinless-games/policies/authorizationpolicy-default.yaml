apiVersion: security.istio.io/v1
kind: AuthorizationPolicy
metadata:
  name: sinless-games-default-allow
  namespace: sinless-games
spec:
  rules:
    - from:
        - source:
            namespaces:
              - sinless-games
              - istio-system
              - monitoring
