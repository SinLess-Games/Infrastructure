# =====================================================================
# pganalyze collector configuration (Managed by Ansible)
# =====================================================================

api_key = {{ postgres_pganalyze_api_key }}

db_name = postgres
db_host = 127.0.0.1
db_username = {{ patroni.superuser.username }}
db_password = {{ postgres_superuser_password }}
