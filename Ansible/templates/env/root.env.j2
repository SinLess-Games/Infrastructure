# =============================================================================
# Local runtime secrets for Infrastructure
# -----------------------------------------------------------------------------
# This file is intentionally gitignored.
# Fill these in locally or rotate them as needed.
# =============================================================================

# Terraform / Proxmox
export TF_VAR_proxmox_api_token_id="{{ lookup('env', 'TF_VAR_proxmox_api_token_id') | default('', true) }}"
export TF_VAR_proxmox_api_token_secret="{{ lookup('env', 'TF_VAR_proxmox_api_token_secret') | default('', true) }}"
