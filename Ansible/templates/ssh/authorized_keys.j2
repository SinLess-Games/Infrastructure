# -------------------------------------------------------------------
# Managed by Ansible — do NOT edit manually
# -------------------------------------------------------------------
# This file is a generated list of SSH public keys authorized for user access
# Format:
#   <optional comment>
#   <public key> <optional key comment>
# -------------------------------------------------------------------

{% set authorized_key_paths = authorized_key_paths | default([]) %}
{% if authorized_key_paths | length > 0 %}
{% for key_path in authorized_key_paths %}
{{ lookup('file', key_path) | trim }}
{% endfor %}
{% else %}
# No authorized keys defined
{% endif %}
