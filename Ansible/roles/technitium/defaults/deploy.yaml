# =============================================================================
# Technitium Deploy Role - Defaults
# =============================================================================

technitium_repo_root: "{{ lookup('ansible.builtin.pipe', 'git rev-parse --show-toplevel') | trim }}"
technitium_terraform_dir: "{{ lookup('ansible.builtin.pipe', 'git rev-parse --show-toplevel') | trim }}/Terraform"
