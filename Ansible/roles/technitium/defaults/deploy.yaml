---
# =============================================================================
# Technitium Deploy Role - Defaults
# =============================================================================

technitium_repo_root: "{{ playbook_dir | dirname | dirname }}"
technitium_terraform_dir: "{{ technitium_repo_root }}/Terraform"
