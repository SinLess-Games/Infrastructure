---
# =============================================================================
# Handlers for Technitium DNS role
# =============================================================================

- name: "Restart systemd-resolved"
  ansible.builtin.systemd:
    name: systemd-resolved
    state: restarted
