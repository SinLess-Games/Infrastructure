---
# Install Ceph packages on designated nodes

- name: Install Ceph packages on Ceph nodes
  ansible.builtin.apt:
    name: "{{ ceph_packages }}"
    state: present
    update_cache: true
  when: inventory_hostname in ceph_nodes
  register: ceph_install_result
  notify:
    - Enable Ceph services
