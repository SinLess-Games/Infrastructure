---
# =============================================================================
# MinIO Deploy Role - Defaults
# =============================================================================

# Repository root (derived from playbook location)
minio_repo_root: "{{ playbook_dir | dirname | dirname }}"

# Terraform directory within repository
minio_terraform_dir: "{{ minio_repo_root }}/Terraform"
