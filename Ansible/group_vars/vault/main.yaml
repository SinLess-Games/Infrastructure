---
# Vault Installation & Version
vault_version: "1.21.3"
vault_user: "vault"
vault_group: "vault"
vault_home: "/etc/vault"
vault_data_dir: "/opt/vault/data"
vault_log_dir: "/var/log/vault"
vault_bin_path: "/usr/local/bin/vault"

# Enable/disable user management
vault_users_enabled: true

# Output directory for generated tokens
vault_users_output_dir: "{{ playbook_dir }}/../../.outputs/vault"

# Proxmox API Configuration
vault_proxmox_api_url: "{{ infra_proxmox_api_url }}"
vault_proxmox_api_token_file: "{{ infra_proxmox_api_token_file }}"
vault_proxmox_tls_insecure: "{{ infra_proxmox_tls_insecure }}"

# Secrets engine configuration (Terraform vault-setup role)
vault_setup_kv_mounts: "{{ vault_kv_mounts }}"
vault_setup_engine_mounts: "{{ vault_engine_mounts }}"
vault_setup_aws_configs: "{{ vault_aws_configs }}"
vault_setup_gcp_configs: "{{ vault_gcp_configs }}"
vault_setup_azure_configs: "{{ vault_azure_configs }}"
vault_setup_linode_configs: "{{ vault_linode_configs }}"
vault_setup_kubernetes_configs: "{{ vault_kubernetes_configs }}"
vault_setup_ssh_configs: "{{ vault_ssh_configs }}"
vault_setup_pki_configs: "{{ vault_pki_configs }}"
vault_setup_transit_configs: "{{ vault_transit_configs }}"
vault_setup_terraform_configs: "{{ vault_terraform_configs }}"
vault_setup_secrets_data: "{{ vault_secrets_data }}"

# Auth methods to enable (configurable)
vault_auth_methods:
  - type: "userpass"
    path: "userpass"
  - type: "ldap"
    path: "ldap"
  - type: "totp"
    path: "totp"
  - type: "approle"
    path: "approle"
  - type: "oidc"
    path: "oidc"
  - type: "jwt"
    path: "jwt"
  - type: "kubernetes"
    path: "kubernetes"

vault_kubernetes_auth_configs:
  - mount_path: "kubernetes"
    kubeconfig_path: "{{ vault_repo_root }}/.outputs/kubernetes/prod/kubeconfig-prod"
    disable_iss_validation: true

vault_kubernetes_secret_engine_configs:
  - mount_path: "kubernetes-dev"
    kubeconfig_path: "{{ vault_repo_root }}/.outputs/kubernetes/dev/kubeconfig-dev"
  - mount_path: "kubernetes-staging"
    kubeconfig_path: "{{ vault_repo_root }}/.outputs/kubernetes/staging/kubeconfig-staging"
  - mount_path: "kubernetes-prod"
    kubeconfig_path: "{{ vault_repo_root }}/.outputs/kubernetes/prod/kubeconfig-prod"

vault_kubernetes_auth_roles:
  - name: "eso-production"
    mount_path: "kubernetes"
    bound_service_account_names:
      - "external-secrets"
    bound_service_account_namespaces:
      - "security"
    policies:
      - "eso-production"
    token_ttl: "1h"

# Vault Cluster Configuration
vault_cluster_name: "sinless-vault"
vault_cluster_nodes: []  # Override in environment-specific files
vault_enable_cluster: true

# TLS/Certificate Configuration
vault_tls_enabled: true
vault_tls_cert_file: "/etc/vault/tls/vault.crt"
vault_tls_key_file: "/etc/vault/tls/vault.key"
vault_tls_ca_cert_file: "/etc/vault/tls/ca.crt"
vault_tls_ca_key_file: "/etc/vault/tls/ca.key"
vault_ca_bundle_file: "/etc/vault/tls/ca-bundle.pem"
vault_tls_min_version: "tls12"

# Raft Storage Configuration
vault_raft_enabled: true
vault_raft_retry_join_enabled: true
vault_raft_autopilot_enabled: true
# NOTE: retry_join is configured per-host in cluster.yaml task to avoid self-join issues

# HAProxy & Keepalived (Load Balancing)
vault_ha_enabled: true
vault_keepalived_enabled: true
vault_haproxy_enabled: true
vault_keepalived_priority: 100  # Override per node
vault_keepalived_virtual_ip: "10.10.10.180"  # Override in environment
vault_keepalived_interface: "eth0"  # Override per environment

# Vault API Listeners
vault_api_addr_protocol: "https"
vault_api_addr_port: 8200
vault_cluster_addr_port: 8201
vault_listener_address: "{{ ansible_host | default(inventory_hostname) }}:{{ vault_api_addr_port }}"
vault_cluster_addr_ip: "{{ ansible_host | default(inventory_hostname) }}"

# Domain Configuration
vault_domain: "local.sinlessgames.com"
vault_cluster_hostname: "vault.{{ vault_domain }}"

# Vault Unseal Configuration
vault_unseal_keys_output_dir: "{{ playbook_dir }}/../../.outputs/vault"
vault_unseal_keys_file: "unseal-keys.json"
vault_unseal_keys_backup: true
vault_unseal_keys_backup_vault: true

# Always allow the configure phase to initialize a brand-new cluster.
# The role still checks Vault's actual init state before attempting init.
vault_initialize: true

# Auto-unseal (Transit) configuration
# Keep `vault_seal_type: "shamir"` until you run migration.
# After migration succeeds, set this to "transit" for steady-state config.
vault_seal_type: "shamir"
vault_transit_address: ""
vault_transit_token: ""
vault_transit_key_name: "autounseal"
vault_transit_mount_path: "transit"
vault_transit_skip_verify: true
vault_transit_disable_renewal: false

# Service Configuration
vault_systemd_service_enabled: true
vault_systemd_restart_policy: "always"
vault_systemd_restart_sec: 5

# Backup & High Availability
vault_backup_raft_snapshots: true
vault_backup_snapshots_dir: "/opt/vault/snapshots"
vault_enable_audit_logging: true
vault_audit_log_file: "{{ vault_log_dir }}/audit.log"

# Default Vault Configuration Options
vault_default_lease_duration: "168h"  # 7 days
vault_max_lease_duration: "720h"      # 30 days
vault_log_level: "info"

# UI Configuration
vault_ui_enabled: true

# Client monitoring / activity counters
vault_client_monitoring_enabled: true
vault_client_monitoring_retention_months: 60
